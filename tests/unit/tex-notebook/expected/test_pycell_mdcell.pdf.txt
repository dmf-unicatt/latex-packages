     Chapter 1

     Case with numbers

     We start from the most simple case possible, the case of numbers.
         Note that this content is typed in markdown, and then converted to LaTeX
     via the markdown package.
1    x = 1
2    y = 2
3    # fmt: off
4    z = 3; n = 4        # noqa: E702
5    # fmt: on

6    l = 6     # noqa: E741
7    m = 7
         We can print a linear combination of the provided number from a pycell,
     either directly...
8    print(x + y + z + n - l - m)
     -3
          ... implicitly ...
9    x + y + z + n - l - m
     -3
          ... or in both ways (with duplicated output)

10   print(x + y + z + n - l - m + 1)
11   x + y + z + n - l - m + 2
     -2
     -1
         Implicit variable display also works when the instruction is split in multiple
     cells
12   print(x + y + z + n - l - m + 1)
13   (x + y + z + n - l - m + 2)
     -2
     -1
          One can also print lists of numbers

14   x = [1, 2, 3]
15   y = [4, 5, 6]
16   print(f"{x}")
17   print(f"{y}")
     [1, 2, 3]
     [4, 5, 6]
     Chapter 2

     Case with strings and
     spaces

     One can print simple strings, possibly containing spaces

1    x = "one"
2    y = "two two"
3    # fmt: off
4    z = "three THREE_Three"; n = "FoUr_four quattro_cuatro"    # noqa:
      ,→ E702
5    # fmt: on
6    f"{x} {y} {z} {n}"
     'one two two three THREE_Three FoUr_four quattro_cuatro'
7    print(f"{x} {y}")
8    print(f"{z} {n}")
     one two two
     three THREE_Three FoUr_four quattro_cuatro
        One can also print multiline strings
9    Z = """three
10   THREE_Three"""
11   N = """FoUr_four
12   quattro_cuatro"""
13   print(f"{Z}\n{N}")
     three
     THREE_Three
     FoUr_four
     quattro_cuatro
14   # fmt: off
15   Z = '''three
16   THREE_Three''' # noqa: Q001
17   N = '''FoUr_four
18   quattro_cuatro''' # noqa: Q001
19   # fmt: on
20   print(f"{Z}\n{N}")
     three
     THREE_Three
     FoUr_four
     quattro_cuatro
    Chapter 3

    Variable resets

    In this file the python session resets at every chapter, and therefore

1   a = 1
2   b = "this is a string"
3   print(a, b)
    1 this is a string
        works, but trying to use the variable x from the previous chapter would not
    (see test_pycell_FAIL.tex).
    Chapter 4

    Figures

    You can have plotly figures in the code cells. By outputting the figure in the
    last line of the code cell it will be automatically added to the document.
1   import plotly.express as px
2

3   fig = px.scatter(x=[1, 2, 3], y=[4, 5, 6])
4   fig




       A figure appears also when other text outputs are present.
5   x = [10, 20, 30]
6   y = [40, 50, 60]
7   print(f"x = {x}")
8    print(f"y = {y}")
9    fig = px.scatter(x=x, y=y)
10   fig
     x = [10, 20, 30]
     y = [40, 50, 60]




       One can customize the figure labels using either HTML or LaTeX labels.
11   import numpy as np
12   import plotly.graph_objects as go
13

14   x_plot = np.linspace(-4 * np.pi, 4 * np.pi, 200)
15   y_plot = 3 * np.sqrt(2) * np.sin(x_plot)
16

17

18   def xlabel(k: float | int) -> str:
19       """Prepare HTML labels on the x axis."""
20       if k == 0:
21           return "0"
22       num = abs(k)
23       s = f"{num}&#960;"
24       s = s if k > 0 else f"-{s}"
25       return s
26

27

28   def ylabel(k: float | int) -> str:
29       """Prepare LaTeX labels on the y axis."""
30       if k == 0:
31           return "0"
32       num = abs(k)
33       s = str(num) + "\\sqrt{2}"
34       s = s if k > 0 else f"-{s}"
35       return "$" + s + "$"
36

37

38   xvals = [k * np.pi for k in range(-4, 5)]
39   xlabels = [xlabel(k) for k in range(-4, 5)]
40   yvals = [k * np.sqrt(2) for k in range(-3, 4)]
41   ylabels = [ylabel(k) for k in range(-3, 4)]
42   fig = go.Figure()
43   fig.add_scatter(x=x_plot, y=y_plot)
44   fig.update_layout(
45       xaxis=dict(
46           title="x",
47           range=[-4 * np.pi, 4 * np.pi],
48           tickmode="array",
49           tickvals=xvals,
50           ticktext=xlabels,
51       ),
52       yaxis=dict(
53           title="y",
54           range=[-3 * np.sqrt(2), 3 * np.sqrt(2)],
55           tickmode="array",
56           tickvals=yvals,
57           ticktext=ylabels,
58       ),
59   )
60   fig
61   fig = go.Figure()
62   fig.add_scatter(x=y_plot, y=x_plot)
63   fig.update_layout(
64       xaxis=dict(
65           title="y",
66           range=[-3 * np.sqrt(2), 3 * np.sqrt(2)],
67           tickmode="array",
68           tickvals=yvals,
69           ticktext=ylabels,
70       ),
71       yaxis=dict(
72           title="x",
73           range=[-4 * np.pi, 4 * np.pi],
74           tickmode="array",
75           tickvals=xvals,
76           ticktext=xlabels,
77       ),
78   )
79   del fig # fails with kaleido>=1, incorrect x axis
Chapter 5

Interacting with the outer
document

mdcell can interact with the rest of the document.
   For instance, we can define an equation and a figure before the mdcell call.

                                  1+1=2                                   (5.1)
   Next, we can refer to them inside the mdcell using `\eqref{...}`{=tex}
and `\ref{...}`{=tex} commands.
   We are inside the markdown cell: refer to (5.1) and to Figure 5.1.




               Figure 5.1: This is a sample figure with caption
    Chapter 6

    Hiding codes

    When PythonTeX is loaded, by providing the optional print argument, some
    pycell can be executed, included in the output notebook, but hidden from the
    print.
        We first show a cell without the print argument, which defines the variable
    a ...
        This is a text commenting the definition of the variable a. The text will
    appear in the notebook and in the pdf.

1   a = 1
       ... then a cell with print=true, which defines the variable b ...
       This is a text commenting the definition of the variable b. The text will
    appear in the notebook and in the pdf.
2   b = 2
       ... and finally a cell with print=false, which defines the variable c but will
    not show up in the pdf.
       The variable c is still available, as we can see by summing a + b + c:
3   a + b + c
    6
        Unfortunately the print optional argument is not supported without PythonTeX.
     Chapter 7

     Customizing printing and
     plotting

     7.1     numpy configuration
     The package internally sets numpy printing with the precision = 3, suppress
     = False, edgeitems = 3, threshold = 12. The user can still customize these
     choices by replacing them with np.set_printoptions. Furthermore, it omits
     types like np.float64 or np.int32 while printing the numerical results: this
     behavior cannot be disabled.
1    import numpy as np
2

3    pi = np.float64(np.pi)
4    pi
     3.142
5    print(pi)
     3.142
6    repr(pi)
     '3.142'
7    pi123 = np.array([pi, 2 * pi, 3 * pi])
8    pi123
     array([3.142, 6.283, 9.425])
9    print(pi123)
     [3.142 6.283 9.425]
10   repr(pi123)
     'array([3.142, 6.283, 9.425])'
     7.2     plotly configuration
     The package also does the following configuration for plotly images:

       • The library trims white space from within the image to save space in the
         LaTeX document: this behavior cannot be configured.
       • If width and height are not assigned, a default value of plotly_default_width
         = 700, plotly_default_height = 500 is assigned. The default value can
         be changed by modifying pytex.plotly_default_width and pytex.plotly_default_height.
       • Before saving, each dimension of the image is rescaled by a factor plotly_default_scale
         = 2. The default value can be changed by modifying pytex.plotly_default_scale.

        We prepare data for the visualization of the function f (x, y) = x2 + 2y 2 on
     the domain D = [−1, 1]2 .
11   x_plot = np.linspace(-1, 1, 100)
12   y_plot = np.linspace(-1, 1, 100)
13   X_plot, Y_plot = np.meshgrid(x_plot, y_plot)
14   Z_plot = X_plot**2 + 2 * Y_plot**2
        We next import plotly. We also add an auxiliary function to change the
     background to black, so that white space trimming has no effect.
15   import plotly.graph_objects as go         # noqa: I001, RUF100
16   def set_black_background(fig: go.Figure) -> None:
17       """Set black background to a plotly figure."""
18       fig.update_layout(
19           paper_bgcolor="black", # figure background
20           plot_bgcolor="black", # plot area background
21           font=dict(color="white"), # make text white
22       )
23   def check_image_size(
24       image_paths: list[str], expected_dimensions: tuple[int, ...]
25   ) -> None:
26       """Ensure that the images have the expected dimensions."""
27       import PIL.Image
28

29         if len(expected_dimensions) == 2:
30             expected_width, expected_height = expected_dimensions
31             expected_width_tolerance = 0
32             expected_height_tolerance = 0
33         else:
34             assert len(expected_dimensions) == 4
35             (
36                 expected_width,
37                 expected_width_tolerance,
38                 expected_height,
39               expected_height_tolerance,
40           ) = expected_dimensions
41       for image_path in image_paths:
42           img = PIL.Image.open(image_path)
43           width, height = img.size
44           assert abs(width - expected_width) <=
              ,→ expected_width_tolerance, (
45               f"Image {image_path} has width "
46               f"{width}, but expected width was "
47               f"{expected_width} with tolerance "
48               f"{expected_width_tolerance}"
49           )
50           assert abs(height - expected_height) <=
              ,→ expected_height_tolerance, (
51               f"Image {image_path} has height "
52               f"{height}, but expected height was "
53               f"{expected_height} with tolerance "
54               f"{expected_height_tolerance}"
55           )
       We save a contour plot using the default width and height.
56   fig_path = os.path.join( # ty: ignore[unresolved-reference]
57       f"pythontex-files-{pytex.jobname}", # ty:
          ,→ ignore[unresolved-reference]
58       f"{pytex.id}_{pytex.instance}.png", # ty:
          ,→ ignore[unresolved-reference]
59   )
60   fig = go.Figure(data=[go.Contour(x=x_plot, y=y_plot, z=Z_plot)])
61   fig.update_layout(title="Contour plot")
62   fig
63   check_image_size(
64       [
65           fig_path,
66           os.path.join( # ty: ignore[unresolved-reference]
67               "images",
                  ,→ "contour_plot_default_width_height_white_background.png"
68           ),
69       ],
70       (1223, 4, 740, 4), # affected by white space trimming
71   )
72   fig_path = os.path.join( # ty: ignore[unresolved-reference]
73       f"pythontex-files-{pytex.jobname}", # ty:
          ,→ ignore[unresolved-reference]
74       f"{pytex.id}_{pytex.instance}.png", # ty:
          ,→ ignore[unresolved-reference]
75   )
76   fig = go.Figure(data=[go.Contour(x=x_plot, y=y_plot, z=Z_plot)])
77   fig.update_layout(title="Contour plot")
78   set_black_background(fig)
79   fig
80   check_image_size(
81       [
82           fig_path,
83           os.path.join( # ty: ignore[unresolved-reference]
84               "images",
                  ,→ "contour_plot_default_width_height_black_background.png"
85           ),
86       ],
87       (1400, 1000), # default 700 * 500, multiplied by 2
88   )

       We next save a contour plot manually assigning width and height.
89   fig_path = os.path.join( # ty: ignore[unresolved-reference]
90       f"pythontex-files-{pytex.jobname}", # ty:
          ,→ ignore[unresolved-reference]
91       f"{pytex.id}_{pytex.instance}.png", # ty:
          ,→ ignore[unresolved-reference]
92   )
93   fig = go.Figure(data=[go.Contour(x=x_plot, y=y_plot, z=Z_plot)])
94   fig.update_layout(title="Contour plot", width=512, height=512,
      ,→ autosize=False)
95   fig
 96   check_image_size(
 97       [
 98           fig_path,
 99           os.path.join( # ty: ignore[unresolved-reference]
100               "images",
                   ,→ "contour_plot_fixed_width_height_white_background.png"
101           ),
102       ],
103       (855, 4, 764, 4), # affected by white space trimming
104   )
105   fig_path = os.path.join( # ty: ignore[unresolved-reference]
106       f"pythontex-files-{pytex.jobname}", # ty:
           ,→ ignore[unresolved-reference]
107       f"{pytex.id}_{pytex.instance}.png", # ty:
           ,→ ignore[unresolved-reference]
108   )
109   fig = go.Figure(data=[go.Contour(x=x_plot, y=y_plot, z=Z_plot)])
110   fig.update_layout(title="Contour plot", width=512, height=512,
       ,→ autosize=False)
111   set_black_background(fig)
112   fig




113   check_image_size(
114       [
115           fig_path,
116           os.path.join( # ty: ignore[unresolved-reference]
117               "images",
                   ,→ "contour_plot_fixed_width_height_black_background.png"
118           ),
119       ],
120       (1024, 1024), # assigned 512 * 512, multiplied by 2
121   )
         We finally change the default width, height and scale, and prepare new
      contour plots.
122   old_plotly_default_width = pytex.plotly_default_width # ty:
       ,→ ignore[unresolved-reference]
123   pytex.plotly_default_width = 555 # ty:
       ,→ ignore[unresolved-reference]
124   old_plotly_default_height = pytex.plotly_default_height # ty:
       ,→ ignore[unresolved-reference]
125   pytex.plotly_default_height = 333 # ty:
       ,→ ignore[unresolved-reference]
126   old_plotly_default_scale = pytex.plotly_default_scale # ty:
       ,→ ignore[unresolved-reference]
127   pytex.plotly_default_scale = 3 # ty:
       ,→ ignore[unresolved-reference]
128   fig_path = os.path.join( # ty: ignore[unresolved-reference]
129       f"pythontex-files-{pytex.jobname}", # ty:
           ,→ ignore[unresolved-reference]
130       f"{pytex.id}_{pytex.instance}.png", # ty:
           ,→ ignore[unresolved-reference]
131   )
132   fig = go.Figure(data=[go.Contour(x=x_plot, y=y_plot, z=Z_plot)])
133   fig.update_layout(title="Contour plot")
134   fig




135   check_image_size(
136       [
137           fig_path,
138           os.path.join(    # ty: ignore[unresolved-reference]
139               "images",
140

                  ,→   "contour_plot_new_default_width_height_white_background.png",
141          ),
142       ],
143       (1396, 4, 608, 4),    # affected by white space trimming
144   )
145   fig_path = os.path.join( # ty: ignore[unresolved-reference]
146       f"pythontex-files-{pytex.jobname}", # ty:
           ,→ ignore[unresolved-reference]
147       f"{pytex.id}_{pytex.instance}.png", # ty:
           ,→ ignore[unresolved-reference]
148   )
149   fig = go.Figure(data=[go.Contour(x=x_plot, y=y_plot, z=Z_plot)])
150   fig.update_layout(title="Contour plot")
151   set_black_background(fig)
152   fig




153   check_image_size(
154       [
155           fig_path,
156           os.path.join(      # ty: ignore[unresolved-reference]
157               "images",
158

                     ,→   "contour_plot_new_default_width_height_black_background.png",
159             ),
160        ],
161        (1665, 999),    # default 555 * 333, multiplied by 3
162   )
          We assign the defaults back.

163   pytex.plotly_default_width = old_plotly_default_width # ty:
       ,→ ignore[unresolved-reference]
164   pytex.plotly_default_height = old_plotly_default_height # ty:
       ,→ ignore[unresolved-reference]
165   pytex.plotly_default_scale = old_plotly_default_scale            # ty:
       ,→ ignore[unresolved-reference]
          Notebooks are automatically produced from this chapter, but any cell con-
      taining pytex would not be runnable in a plain notebook, because the pytex
      object is internal. Running the produced notebook is not really the point of
      this test, hence we simply disable any code cell that includes the words pytex
      or fig_path.
166   import typing
167

168   original_save_cells_as_notebook = save_cells_as_notebook            # ty:
       ,→ ignore[unresolved-reference]
169

170

171   def save_cells_as_notebook(
172       all_cells: list[tuple[str, str]],
173       pytex: typing.Any, # noqa: ANN401
174   ) -> None:
175       """Disable code cells containing pytex or fig_path."""
176       modified_all_cells = []
177       for cell in all_cells:
178           if cell[0] == "markdown":
179               modified_all_cells.append(cell)
180           else:
181               assert cell[0] == "code"
182               if "pytex" in cell[1]:
183                   code = """
184   # fmt: off
185   print(
186       'This cell was disabled because it contains the command
           ,→ pytex' # noqa: Q000
187   )
188   # fmt: on"""
189               elif "fig_path" in cell[1]:
190                   code = """
191   # fmt: off
192   print(
193       'This cell was disabled because it contains the variable
           ,→ fig_path' # noqa: Q000
194   )
195   # fmt: on""" # noqa: E501
196               else:
197                   code = cell[1]
198               modified_all_cells.append(("code", code))
199       return original_save_cells_as_notebook(modified_all_cells,
           ,→ pytex)
