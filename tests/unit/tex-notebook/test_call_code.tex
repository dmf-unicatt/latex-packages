\documentclass[oneside]{book}
\usepackage{tests-tex-notebook}

\begin{document}

\chapter{Testing the python function \texttt{call\_code}}

This file tests the python function \texttt{call\_code}, which is defined internally the package.

First of all we define a list that will contain all codes we test, and the local variables they define. These objects mimick the internal implementation used in the package, but choosing different variable names in order not to overlap.

\begin{pycell}
all_codes = []
local_variables = {}

source_file = "test_call_code.tex"
source_line = 1  # mock line
\end{pycell}

Then we run several tests.

\begin{pycell}
cell_output, cell_figure_path = call_code(  # ty: ignore[unresolved-reference]
    """
x = 1
y = 2
""",
    all_codes,
    local_variables,
    pytex,  # ty: ignore[unresolved-reference]
    source_file,
    source_line,
)
assert len(all_codes) == 1
assert all_codes[0]["source_file"] == source_file
assert all_codes[0]["source_line"] == source_line
assert all_codes[0]["tex_backed"] is True
assert all_codes[0]["type"] == "code"
assert (
    all_codes[0]["content"]
    == """
x = 1
y = 2
"""
)
assert cell_output == ""
assert cell_figure_path is None
assert "cell_output" not in local_variables
assert "cell_figure_path" not in local_variables
assert "x" in local_variables
assert "y" in local_variables
assert local_variables["x"] == 1
assert local_variables["y"] == 2
\end{pycell}

\begin{pycell}
cell_output, cell_figure_path = call_code(  # ty: ignore[unresolved-reference]
    """
z = 3; n = 4
""",
    all_codes,
    local_variables,
    pytex,  # ty: ignore[unresolved-reference]
    source_file,
    source_line,
)
assert len(all_codes) == 2
assert all_codes[0]["type"] == "code"
assert (
    all_codes[0]["content"]
    == """
x = 1
y = 2
"""
)
assert all_codes[1]["type"] == "code"
assert (
    all_codes[1]["content"]
    == """
z = 3; n = 4
"""
)
assert cell_output == ""
assert cell_figure_path is None
assert "cell_output" not in local_variables
assert "cell_figure_path" not in local_variables
assert "x" in local_variables  # previous variables are kept
assert "y" in local_variables
assert "z" in local_variables
assert "n" in local_variables
assert local_variables["x"] == 1
assert local_variables["y"] == 2
assert local_variables["z"] == 3
assert local_variables["n"] == 4
\end{pycell}

\begin{pycell}
cell_output, _ = call_code(  # ty: ignore[unresolved-reference]
    """
print(x + y + z + n)
""",
    all_codes,
    local_variables,
    pytex,  # ty: ignore[unresolved-reference]
    source_file,
    source_line,
)
assert cell_output == "10"
\end{pycell}

\begin{pycell}
cell_output, _ = call_code(  # ty: ignore[unresolved-reference]
    """
x + y + z + n
""",
    all_codes,
    local_variables,
    pytex,  # ty: ignore[unresolved-reference]
    source_file,
    source_line,
)
assert cell_output == "10"
\end{pycell}

\begin{pycell}
cell_output, _ = call_code(  # ty: ignore[unresolved-reference]
    """
print(x + y + z + n + 1)
x + y + z + n + 2
""",
    all_codes,
    local_variables,
    pytex,  # ty: ignore[unresolved-reference]
    source_file,
    source_line,
)
assert cell_output == "11\n12"
\end{pycell}

\begin{pycell}
cell_output, _ = call_code(  # ty: ignore[unresolved-reference]
    """
x = "one"
y = "two two"
""",
    all_codes,
    local_variables,
    pytex,  # ty: ignore[unresolved-reference]
    source_file,
    source_line,
)
assert cell_output == ""
\end{pycell}

\begin{pycell}
cell_output, _ = call_code(  # ty: ignore[unresolved-reference]
    """
z = "three THREE_Three"; n = "FoUr_four quattro_cuatro"
""",
    all_codes,
    local_variables,
    pytex,  # ty: ignore[unresolved-reference]
    source_file,
    source_line,
)
assert cell_output == ""
\end{pycell}

\begin{pycell}
cell_output, _ = call_code(  # ty: ignore[unresolved-reference]
    """
" ".join([x, y, z, n])
""",
    all_codes,
    local_variables,
    pytex,  # ty: ignore[unresolved-reference]
    source_file,
    source_line,
)
assert cell_output == (
    "'one two two three THREE_Three FoUr_four quattro_cuatro'"
)
\end{pycell}

\begin{pycell}
cell_output, _ = call_code(  # ty: ignore[unresolved-reference]
    """
print(" ".join([x, y]))
print(" ".join([z, n]))
""",
    all_codes,
    local_variables,
    pytex,  # ty: ignore[unresolved-reference]
    source_file,
    source_line,
)
assert cell_output == (
    "one two two\nthree THREE_Three FoUr_four quattro_cuatro"
)
\end{pycell}

\begin{pycell}
try:
    call_code(  # ty: ignore[unresolved-reference]
        """
raise RuntimeError("a runtime error")
""",
        all_codes,
        local_variables,
        pytex,  # ty: ignore[unresolved-reference]
        source_file,
        source_line,
    )
except RuntimeError as e:
    assert str(e) == "a runtime error"
else:
    assert False, "This should have failed"
\end{pycell}

\begin{pycell}
cell_output, cell_figure_path = call_code(  # ty: ignore[unresolved-reference]
    """
import plotly.express as px
fig = px.scatter(x=[1, 2, 3], y=[4, 5, 6])
fig
""",
    all_codes,
    local_variables,
    pytex,  # ty: ignore[unresolved-reference]
    source_file,
    source_line,
)
assert cell_output == ""
assert cell_figure_path == (
    "pythontex-files-test_call_code/py_default_1_79.png"
)
\end{pycell}

Notebooks are automatically produced from this file, but they would not be runnable because they would miss the definition of \texttt{call\_code}, which is defined internally by this package. This is not really the point of this test, so we simply mock \texttt{save\_cells\_as\_notebook}, also defined internally by this package, to write a mock notebook instead of the actual one.

\begin{pycell}
import typing

original_save_cells_as_notebook = save_cells_as_notebook  # ty: ignore[unresolved-reference]


def save_cells_as_notebook(
    all_cells: list[dict[str, typing.Any]],
    pytex: typing.Any,  # noqa: ANN401
) -> None:
    """Write a mock notebook instead of the actual one."""
    mock_cells = []

    texsync_append_generated_cell(  # ty: ignore[unresolved-reference]
        mock_cells,
        "markdown",
        "This notebook was disabled",
        pytex,
        "test_call_code.tex",
        1,
    )
    texsync_append_generated_cell(  # ty: ignore[unresolved-reference]
        mock_cells,
        "code",
        """
# fmt: off
print('This notebook was disabled')  # noqa: Q000
# fmt: on""",
        pytex,
        "test_call_code.tex",
        1,
    )

    return original_save_cells_as_notebook(mock_cells, pytex)
\end{pycell}

\end{document}
